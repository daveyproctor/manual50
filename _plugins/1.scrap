# module Reading
#   class Generator < Jekyll::Generator
#     def generate(site)
#       ongoing, done = Book.all.partition(&:ongoing?)

#       reading = site.pages.detect {|page| page.name == 'default.html'}
#       reading.data['ongoing'] = ongoing
#       reading.data['done'] = done
#     end
#   end
# end